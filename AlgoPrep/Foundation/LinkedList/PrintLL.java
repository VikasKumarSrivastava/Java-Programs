class Solution{
  class Node{
    int val;
    Node next;
    Node(int v){
      val =v;
      next = NULL:
        }
  }
    public void static display(Node head){
      Node temp = head;
      while(temp!=NULL){
        System.out.print(temp.data+" ");
        temp = temp.next;
      }
  }
}
